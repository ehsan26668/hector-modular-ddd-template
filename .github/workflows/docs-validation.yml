name: Docs Validation

on:
  pull_request:
    branches: [main]
  push:
    branches: [main]

jobs:
  docs-validation:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup .NET
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: 10.0.x

      - name: Restore
        run: dotnet restore Hector.slnx

      - name: Build
        run: dotnet build Hector.slnx --no-restore --configuration Release

      - name: Run Documentation and ADR Tests
        run: dotnet test tests/ArchitectureTests/Hector.ArchitectureTests/Hector.ArchitectureTests.csproj --no-build --configuration Release --filter "FullyQualifiedName~Documentation|FullyQualifiedName~Adr|FullyQualifiedName~Structure"
